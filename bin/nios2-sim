#!/usr/bin/env node
var path      = require("path");
var fs        = require("fs");
var src       = path.join(__dirname, "../lib/src");
var Simulator = require(path.join(src, "simulator")).Simulator;
new Simulator().run(process.argv);
