#!/usr/bin/env node
var path      = require("path");
var fs        = require("fs");
var lib       = path.join(path.dirname(fs.realpathSync(__filename)), "../lib");
var Simulator = require(path.join(lib, "simulator")).Simulator;
new Simulator().run(process.argv);
